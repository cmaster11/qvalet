# This defaults file is to be used together with the `config.combined.echo.yaml` config via:
#
# qvalet -c config.combined.echo.yaml -c config.combined.echo.part2.yaml -f defaults.combined.echo.yaml
#

command: echo